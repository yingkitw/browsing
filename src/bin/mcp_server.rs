//! MCP (Model Context Protocol) server for browsing
//!
//! Note: This is a placeholder implementation. The rmcp crate API is still evolving.
//! For a production MCP server, consider implementing the MCP protocol directly
//! or using a stable MCP server framework.

fn main() {
    eprintln!("MCP server implementation is currently under development.");
    eprintln!("The rmcp crate API is evolving and this implementation needs to be updated.");
    eprintln!("\nFor now, please use the CLI or library interfaces:");
    eprintln!("  CLI: cargo run --bin browsing -- run \"task description\"");
    eprintln!("  Library: See examples/library_usage.rs");
    std::process::exit(1);
}
